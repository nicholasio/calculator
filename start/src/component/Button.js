// @todo import React, PropTypes and Button.css

// create a class compopnent that when clicked will call props.clickHandler passing its name.
// the component should also accept a orange prop that will render the button with a orange background


// @todo define the prop types

// @todo export the componetn
